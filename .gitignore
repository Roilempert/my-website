# macOS
.DS_Store
.AppleDouble
.LSOverride

# Cursor debug logs
.cursor/debug-*.log

# Editor local state (keep shared settings if you add them)
.vscode/*.code-workspace

# Debug screenshots (agent captures — not part of the site)
/site-*.png
/site-full.png
/site-viewport.png

# Playwright MCP capture artifacts (agent test output — not part of the site)
/.playwright-mcp/

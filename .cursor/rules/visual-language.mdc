---
description: Exhibition visual language — read and update docs/visual-language.md when changing UI tokens, chrome, or SVG assets
globs: styles.css,js/warehouse-core.js,js/navigation-map.js,js/artifact-inspector.js,assets/ui/**,docs/visual-language.md
alwaysApply: false
---

# Visual language (exhibition UI)

## Before changing chrome

Read [`docs/visual-language.md`](../../docs/visual-language.md) for locked colors, type classes, spacing, grid marks, and component rules (warehouse, layer nav, L3 notes, inspector).

Target: **21.5″ iMac @ 1920×1080**. Shell sizes use **`VISUAL_SCALE = 1.0`** (see doc).

When the user gives Figma measurements in `px`, translate them automatically:

- Component spacing/padding/insets/radii in 5px jumps → `--space-*` tokens (`5px` → `var(--space-5)`, `10px` → `var(--space-10)`, `15px` → `var(--space-15)`, `20px` → `var(--space-20)`, `30px` → `var(--space-30)`, `40px` → `var(--space-40)`, `60px` → `var(--space-60)`).
- Shell/grid layout → `--site-grid-*` or `calc(N * var(--site-grid-cell-w))`.
- Raw `px` is acceptable only for hairlines/borders, SVG primitives, physics/canvas coordinates, and tiny debug-only values.

## After any visual change

Update **`docs/visual-language.md` in the same session**:

- Token or rule changed → edit the relevant section + add a **Changelog** entry (date, what changed).
- New SVG under `assets/ui/` → add row to **SVG assets** table.
- New component chrome → add or extend **Components** section.

Do not leave code and legend out of sync. Same discipline as `docs/CHECKPOINT.md` for physics.

## Site content

Hebrew UI strings and note data stay Hebrew — do not translate. This doc describes **chrome and typography roles**, not note body copy from the sheet.
